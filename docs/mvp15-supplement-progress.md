# MVP1.5 에이전트 보완 마무리

기준: `feature/mvp-1.5-orchestra`, 2026-09-26. 기존 미커밋 작업을 보존한다.

## 범위

- 최종판단 계약 실패 진단, 원인 확인, 보완 없음·성공·실패 검증.
- 기존 상권·개폐업 부분 보완 함수를 재사용한다.
- 사용자 질문·LangGraph·신규 에이전트·HTTP 연결은 제외한다.
- 커밋·푸시는 하지 않는다.

## 진행

- [x] 업종·근거 검증 오류를 원문 없이 구분하는 진단과 회귀 시험
- [x] 기존 저장 분석을 재사용해 새 응답의 실패 원인 확인
- [x] 보완 없음·성공·실패 시 재판단·저장 대역 통합 검증
- [x] 전체 회귀·정적 검사 및 독립 검토
- [ ] 실제 LLM의 빈 근거 인용 대응 및 실제 보완 완료 검증

## 확인한 사실

- 9월 23일 실행은 세 분석을 저장한 뒤 최종판단 계약 검증에서 실패했다.
- 당시 모델 응답은 저장하지 않아 그 응답의 정확한 오류를 사후 복원할 수 없다.
- 재실행 결과는 새 응답에 대한 검증이며 과거 오류와 동일하다고 단정하지 않는다.
- 현재 일반 ValueError는 validation 도구에서 INPUT_OR_CONTRACT_INVALID로 합쳐진다.

## 작업 결정

- 기존 검증을 완화하지 않고 안전한 오류 코드·결과 필드 위치를 추가한다.
- 원문 프롬프트·모델 자유 문자열·비밀키는 진단에 남기지 않는다.
- 기존 작업 브랜치에서 이어가며 별도 worktree로 미커밋 구현을 분리하지 않는다.

## 이번 변경

- `decision/agent.py`: ValueError 호환 진단 오류에 고정 사유·결과 필드 위치 추가.
- `validation_tool/trace.py`: 업종·근거 오류를 일반 입력 오류와 구분해 기록.
- 두 회귀 시험: 오류 위치와 원문 비노출, 실패 시 분석 3개 보존 검증.
- 오류 허용·빈 근거 자동 삭제는 하지 않는다. 출력 교정은 아래 후속 작업으로 추가했다.

## 검증 기록

- Python 3.12: 백엔드 323개, validation 19개 통과.
- 보완 루프 19개 및 실제 함수의 API 대역 통합 4개는 위 백엔드 집계에 포함.
- 백엔드 Ruff, 변경 파일 포맷, mypy 69개 소스 통과.
- validation lint는 `--config backend/pyproject.toml`로 프로젝트 규칙 적용 시 통과.
- 기존 Starlette/httpx 사용 중단 예정 경고는 유지. 이번 범위에서 의존성 변경 안 함.
- 독립 검토: 보완 원본 보존·1회 제한·안전한 오류 진단에서 중요한 결함 없음.

## 실제 최종판단 재시험

- 실행 ID: `bd4782ac757d4900a5fbeb50931c91d9`
- 입력은 9월 23일 저장된 분석 재생. 주소·초기 분석·오케스트레이터 선택은 대역이며 최종판단 LLM은 실제 호출.
- 실제 보완 함수는 등록했으나 모델이 바로 final을 선택하여 실행되지 않음.
- 실패: `DECISION_CONTRACT_INVALID`, 사유 `evidence_empty`.
- 위치: `not_recommended.0.evidence.1.path` — 비추천 첫 항목의 두 번째 근거가 빈 자료를 인용.
- 원본 분석은 보존되고 최종 결과는 저장되지 않음. 오류 차단은 정상이며 전체 성공은 아님.
- 구체적인 원문 값은 로그에 남기지 않음. 9월 23일 실패와 같은 오류라고 단정하지 않음.

## 출력 교정 구현

사용자 승인 후 업종·근거 계약 오류에 한해 같은 분석 자료와 검증 위치·사유, 이전 판단을 모델에 전달하여 최대 1회 교정하도록 구현했다.

- 첫 결과가 유효하면 교정 호출 없음.
- 두 번째 응답도 잘못되면 실패. 전송·타임아웃·JSON 및 스키마 오류는 이 교정 대상이 아님.
- 교정 중 데이터 보완 전환 금지. 데이터 보완 1회 예산과 별개이며 원본 분석 재실행 없음.
- trace의 decision_model started 이벤트에 correction 여부 표시. 이전 판단 원문은 trace에 기록하지 않음.
- 회귀: 백엔드 327개·validation 20개 통과. Ruff·변경 파일 포맷·mypy 69개 소스 통과.
- 독립 검토에서 중요한 회귀 없음.

## 후속 실제 시험

- 실행 ID: `225b6cabbef146419dfa34f00aa9fdc0`.
- 동일 저장 분석 + 실제 최종판단 LLM. 주소·초기 분석·오케스트레이터 선택은 재생/대역.
- 첫 모델 응답이 유효하여 교정 및 데이터 보완 미실행.
- 요청 completed, 최종 결과 partial, 분석 3개·최종판단 1개 저장 확인.
- 실제 LLM 교정 성공 및 실제 API 보완 후 완료를 입증한 시험은 아님. 해당 경계는 대역 시험으로 검증했으며 실환경 확인은 남음.
